export default function Progress() {
  return (
    <section className="space-y-4">
      <div className="card-surface max-w-2xl">
        <h2 className="section-title">Progress snapshots</h2>
        <p className="mt-2 text-sm text-[var(--text-secondary)]">
          Your progress visuals can be added here. Layout and typography are now
          aligned with the app-wide UI updates.
        </p>
      </div>
    </section>
  );
}
